# apply.md

Apply an approved patch plan to canonical docs.

If the patch plan is mechanically healthy but semantically too thin, record an apply-stage note and run `specops supersede-synthesis <run-id> --from <spec_delta.json>` before applying.
